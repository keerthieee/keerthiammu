#include <stdio.h>

int main(void) {
	int k,v,sum;
	scanf("%d",&k);
	
	for(v=1;v<=i;v++)
	{
		
			if(k%v==0)
		{	
			sum++;
			
		}
	}
		if(sum==2)
		

	printf("it is prime number");
else

printf("not a prime");


	return 0;
}
